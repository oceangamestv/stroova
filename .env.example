# URL бэкенда API (обязательно; без слэша в конце).
# Все данные пользователей (аккаунты, прогресс, «Мой словарь») хранятся на сервере.
# Локально: скопируйте в .env и задайте VITE_API_URL=http://localhost:3000/api
# На сервере: VITE_API_URL=https://ВАШ_ДОМЕН/api (нужен при сборке фронта)
VITE_API_URL=https://stroova.ru/api

# --- Только на сервере (для запуска API) ---
# PORT=3000
# CORS_ORIGIN=https://ВАШ_ДОМЕН
# PostgreSQL (обязательно для API):
# DATABASE_URL=postgresql://USER:PASSWORD@HOST:5432/DATABASE
# Пример: postgresql://stroova:secret@localhost:5432/stroova
DATABASE_URL=