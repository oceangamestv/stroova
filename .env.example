# URL бэкенда API задаётся по режиму (без слэша в конце):
#   npm run dev              → .env.development (локальный http://localhost:3000/api)
#   npm run build            → .env.production (боевой сервер)
#   npm run build:local      → сборка с локальным API (для тестового APK)
# Локальные переопределения (не коммитить): .env и .env.local
# См. .env.development и .env.production в корне.
# Локально: см. .env.development (http://localhost:3000/api). Боевой: https://stroova.ru/api
VITE_API_URL=https://stroova.ru/api

# --- Только на сервере (для запуска API) ---
# PORT=3000
# CORS: через запятую — URL сайта и мобильное приложение (Capacitor):
# CORS_ORIGIN=https://stroova.ru,https://www.stroova.ru,capacitor://localhost,http://localhost
# PostgreSQL (обязательно для API):
# DATABASE_URL=postgresql://USER:PASSWORD@HOST:5432/DATABASE
# Пример: postgresql://stroova:secret@localhost:5432/stroova
# ВАЖНО: не храните реальные пароли в репозитории.
DATABASE_URL=postgresql://stroova:CHANGE_ME@localhost:5432/stroova

# --- AI (опционально, для админки словаря) ---
# OpenAI-совместимые провайдеры:
# OPENAI_BASE_URL=https://api.openai.com/v1
# OPENAI_API_KEY=sk-...
# OPENAI_MODEL=gpt-4o-mini
# Примечание: ChatGPT Plus НЕ даёт API-кредиты — нужен отдельный API billing.
# При ошибке 401 invalid_api_key: проверьте ключ на https://platform.openai.com/account/api-keys
# (создайте новый ключ при необходимости; в .env не должно быть пробелов/переносов в значении).

# --- Telegram-бот (опционально) ---
# Токен от @BotFather. Запуск бота: npm run telegram-bot
# TELEGRAM_BOT_TOKEN=123456:ABC-DEF...
# Ссылка в ответах бота (по умолчанию боевой сайт):
# APP_URL=https://stroova.ru