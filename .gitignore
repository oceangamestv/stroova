# Локальные переменные (на сервере создаётся свой .env)
.env
.env.local

# Данные сервера (учётки и прогресс) — не коммитить
server/data.json

# Зависимости и сборка
node_modules/
dist/

# Vite
.vite/

# Capacitor / Android — сборки, кэш и сгенерированные файлы (не синхронизировать с гитом)
android/app/build/
android/build/
android/.gradle/
android/.cxx/
android/.externalNativeBuild/
android/capacitor/build/
android/capacitor-cordova-android-plugins/
android/captures/
android/local.properties
android/app/src/main/assets/
*.apk
*.aab

# IDE
.idea/
*.iml

# Логи и кэш
*.log
.cache/

# Озвучка словаря (Bella/Michael) — не в репо, загружается на сервер по FTP/SCP
public/audio/
