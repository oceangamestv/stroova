# DE microserver (AI worker only, no public app)

NODE_ENV=production

# Calls RF internal endpoint
RF_SYNC_URL=https://your-rf-domain.ru/api/internal/dictionary-upserts
RF_SYNC_SHARED_SECRET=CHANGE_ME_LONG_RANDOM_SECRET
RF_SYNC_SOURCE=de-ai-worker
RF_SYNC_RETRIES=4

# AI provider settings used by worker
OPENAI_API_KEY=CHANGE_ME
OPENAI_BASE_URL=https://api.openai.com/v1
OPENAI_MODEL=gpt-4o-mini

# Optional outbound proxy
# HTTPS_PROXY=http://127.0.0.1:3128
